{
  "name": "clea-chat",
  "version": "1.0.0",
  "description": "clea.chat — Clea's public face and private dashboard",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build": "cd client && npm install && npm run build",
    "test": "vitest run"
  },
  "dependencies": {
    "cookie-parser": "^1.4.6",
    "cron-parser": "^5.5.0",
    "cronstrue": "^3.13.0",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "node-fetch": "^3.3.2",
    "pg": "^8.11.3"
  },
  "engines": {
    "node": ">=18"
  },
  "type": "module",
  "devDependencies": {
    "supertest": "^7.2.2",
    "vitest": "^4.0.18"
  }
}
